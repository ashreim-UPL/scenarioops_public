Fix the strategy to satisfy missing fields and align to the scenarios and company profile.
Return ONLY a JSON object that matches the Strategy Item schema. No prose.

Rules:
- Use only company information provided in Context (company_profile, annual report summary, focal issue). Do not introduce outside facts.
- Ensure name, objective, actions, and KPIs are present.