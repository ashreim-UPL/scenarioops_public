Generate strategies aligned to the provided scenarios and company profile.
Return ONLY a JSON object with a single root key "strategies" containing the list of strategy objects. No prose, no markdown, no extra keys.

Rules:
- Use only company information provided in Context (company_profile, annual report summary, focal issue). Do not introduce outside facts.
- Each strategy must include actions and KPIs.
- Use scenario_ids from Context to ensure alignment.
- Provide clear objectives and measurable KPIs.